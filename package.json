{
  "name": "transparentize",
  "version": "0.1.0",
  "description": "Small set of tools (LIBs & CLI) to remove a color components from images, replacing it with transparency (alpha channel)",
  "workspaces": [
    "packages/*"
  ],
  "repository": "https://github.com/francesco-carrella/transparentize",
  "license": "Commons Clause",
  "private": true,
  "author": {
    "name": "Francesco Carrella",
    "email": "francesco.carrella@gmail.com"
  },
  "scripts": {
    "common": "yarn workspace @transparentize/common",
    "image": "yarn workspace @transparentize/image",
    "web": "yarn workspace @transparentize/web"
  },
  "devDependencies": {
    "eslint": "^8.16.0",
    "eslint-gitignore": "^0.1.0"
  },
  "dependencies": {
  },
  "engines": {
    "node": ">=10"
  }
}
